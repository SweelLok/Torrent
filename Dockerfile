# Вибір базового образу, який буде наслідуватись
FROM python:3.12-slim

# Встановлення робочої директорії в контейнері
WORKDIR  /app

# Копіювання всіх файлів застосунку з компʼютера до директорії /app всередині образу
COPY . /app/

# Інсталяція залежностей застосунку
RUN pip install --no-cache-dir -r requirements.txt

# Вказівка порту, який буде відкритий
EXPOSE 5000
EXPOSE 80

# Команда для запуску застосунку
ENV FLASK_APP=main.py
CMD ["flask", "run", "--host=0.0.0.0"]